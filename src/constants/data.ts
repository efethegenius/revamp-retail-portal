export const OTP_LENGTH = 6;
export const MAX_LOGIN_ATTEMPTS = 5;
export const SESSION_TIMEOUT = 30; // in minutes

export const loadingButtonLabel = "Loading...";

export let isDark = false;

export const setIsDark = (value: boolean) => {
  isDark = value;
};
